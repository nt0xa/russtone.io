extends default

block append head
  include disqus.pug

block content
  +b.ARTICLE.post
    +e.H1.title= page.title
    +e.date= page.date.format('DD MMM YYYY')
    +e.divider
    +e.content!= page.content

  +b.DIV(id='disqus_thread')


